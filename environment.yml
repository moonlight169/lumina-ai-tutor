name: edu-buddy-ai

channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.10
  - numpy
  - requests
  - websockets
  - python-dotenv
  - python-sounddevice
  - pip:
      - faster-whisper
      - openai
      - pyttsx3
      - elevenlabs
      - google-genai
      - openai
      - ollama
      - soundfile
      - sounddevice
      - pydub
      - setuptools
      - nvidia-cublas-cu12
      - nvidia-cudnn-cu12
