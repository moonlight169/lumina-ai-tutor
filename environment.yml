name: edu-buddy-ai

channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.10
  - numpy
  - requests
  - websockets
  - python-dotenv
  - pip

  - pip:
      - faster-whisper
      - openai
      - pyttsx3
      - elevenlabs
      - google-genai
      - openai
      - ollama
      - soundfile
      - sounddevice